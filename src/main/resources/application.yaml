server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/consort_group
    username: consort
    password: 2231251fsad1
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

kafka:
  bootstrap-servers: localhost:9092
  consumer-group-id: email-group
  session-timeout-ms: 10000
  max-partition-fetch-bytes: 1048576
  max-poll-records: 500
  max-poll-interval-ms: 300000
  user-registration: user-registered-topic

mail:
  host: smtp.gmail.com
  port: 587
  username: doniyor.kurbanov.21@gmail.com
  password: sjqb zmfg vwfk gybu
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
  subjectForEmail: "Registration"